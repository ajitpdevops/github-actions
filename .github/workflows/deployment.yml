name: CD
on: [push]
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3

            - name: CD to workdir
              run: cd ./node-app

            - name: Install dependencies
              run: npm ci

            - name: Linting actions
              run: npm run lint
          